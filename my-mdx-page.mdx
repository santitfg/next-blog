import Button from "@/components/button";
import Post from "@/components/Post.jsx";
import Image from 'next/image' 

export const data = {
  title: "hola mdx",
  author: "santi",
  date: "2023-01-22",
};

# My MDX page

I **love** using [Next.js](https://nextjs.org/)

This is a list in markdown:

- One
- Two
- Three

Checkout my React component:
 <Image  src="/imgs/Instagram_Glyph_Gradient.png"   height={144}  width={144}   alt="instagram logo"/>
<Button />

export default ({ children }) => <Post content={children} data={meta} />;

;
